# DataBuff APM 本地开发 — Doris + ingest/web/demo（JAR 目录挂载，改 jar 后 restart 即可）
# 启动: ./start.sh  |  停止: ./stop.sh  |  Doris 重装: ./install.sh

services:
  ai-apm-kafka:
    image: apache/kafka:3.7.0
    container_name: ai-apm-kafka
    hostname: ai-apm-kafka
    restart: unless-stopped
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@ai-apm-kafka:9093
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_LOG_DIRS: /var/lib/kafka/data
    ports:
      - "9092:9092"
    volumes:
      - ./data/kafka:/var/lib/kafka/data
    networks:
      apm:
        ipv4_address: 172.20.80.8
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/9092'"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  ai-apm-doris-fe:
    image: ${DORIS_FE_IMAGE:-apache/doris:fe-4.1.1}
    container_name: ai-apm-doris-fe
    hostname: ai-apm-doris-fe
    restart: unless-stopped
    working_dir: /opt/apache-doris
    environment:
      FE_SERVERS: fe1:172.20.80.2:9010
      FE_ID: "1"
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        sed -i 's/-Xmx8192m/-Xmx512m/g' /opt/apache-doris/fe/conf/fe.conf
        sed -i 's/-Xms8192m/-Xms512m/g' /opt/apache-doris/fe/conf/fe.conf
        exec bash init_fe.sh
    volumes:
      - ./data/fe-meta:/opt/apache-doris/fe/doris-meta
    ports:
      - "8030:8030"
      - "9030:9030"
    networks:
      apm:
        ipv4_address: 172.20.80.2
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/9030' && curl -sf http://127.0.0.1:8030/api/bootstrap"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 120s

  ai-apm-doris-be:
    image: ${DORIS_BE_IMAGE:-apache/doris:be-4.1.1}
    container_name: ai-apm-doris-be
    hostname: ai-apm-doris-be
    restart: unless-stopped
    working_dir: /opt/apache-doris
    environment:
      FE_SERVERS: fe1:172.20.80.2:9010
      BE_ADDR: 172.20.80.5:9050
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        sed -i 's/-Xmx2048m/-Xmx512m/g' /opt/apache-doris/be/conf/be.conf
        exec bash entry_point.sh
    depends_on:
      ai-apm-doris-fe:
        condition: service_healthy
    volumes:
      - ./data/be-storage:/opt/apache-doris/be/storage
    ports:
      - "8040:8040"
    networks:
      apm:
        ipv4_address: 172.20.80.5
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:8040/api/health"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 120s

  ai-apm-ingest:
    image: ${LOCAL_JDK_IMAGE:-databuff-local/jdk-dev:17-jammy}
    container_name: ai-apm-ingest
    hostname: ai-apm-ingest
    restart: unless-stopped
    working_dir: /app
    entrypoint: ["/bin/sh", "/app/start.sh"]
    environment:
      TZ: Asia/Shanghai
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx300m -XX:+ExitOnOutOfMemoryError -Duser.timezone=Asia/Shanghai
      DORIS_FE_HOST: ai-apm-doris-fe
      DORIS_BE_HTTP_HOST: ai-apm-doris-be
      NGINX_KAFKA_BOOTSTRAP: ai-apm-kafka:9092
      NGINX_KAFKA_TOPIC: nginx-access-log
      NGINX_KAFKA_GROUP: databuff-nginx-ingest
    volumes:
      - ./run/ingest:/app
    ports:
      - "${INGEST_GRPC_PORT:-4317}:4317"
      - "${INGEST_HTTP_PORT:-4318}:4318"
      - "${INGEST_SKYWALKING_PORT:-11800}:11800"
    depends_on:
      ai-apm-doris-fe:
        condition: service_healthy
      ai-apm-doris-be:
        condition: service_healthy
      ai-apm-kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/4318'"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 60s
    networks:
      apm:
        ipv4_address: 172.20.80.3

  ai-apm-web:
    image: ${LOCAL_JDK_IMAGE:-databuff-local/jdk-dev:17-jammy}
    container_name: ai-apm-web
    hostname: ai-apm-web
    restart: unless-stopped
    working_dir: /app
    entrypoint: ["/bin/sh", "/app/start.sh"]
    environment:
      TZ: Asia/Shanghai
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx300m -Duser.timezone=Asia/Shanghai
      DORIS_FE_HOST: ai-apm-doris-fe
      DORIS_BE_HTTP_HOST: ai-apm-doris-be
    volumes:
      - ./run/web:/app
      - ../common/skills:/app/skills:ro
      - ../../ai-apm-web/data:/app/data
    ports:
      - "${WEB_HTTP_PORT:-27403}:27403"
    depends_on:
      ai-apm-doris-fe:
        condition: service_healthy
      ai-apm-doris-be:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/27403'"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 90s
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      apm:
        ipv4_address: 172.20.80.4

  ai-apm-demo:
    image: ${LOCAL_JDK_IMAGE:-databuff-local/jdk-dev:17-jammy}
    container_name: ai-apm-demo
    hostname: ai-apm-demo
    restart: unless-stopped
    working_dir: /app
    entrypoint: ["/bin/sh", "/app/start.sh"]
    environment:
      TZ: Asia/Shanghai
      JAVA_TOOL_OPTIONS: -Xms64m -Xmx128m -Duser.timezone=Asia/Shanghai
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://ai-apm-ingest:4318}
      SEED_PROTOCOL: ${SEED_PROTOCOL:-otlp}
      SKYWALKING_GRPC_TARGET: ${SKYWALKING_GRPC_TARGET:-ai-apm-ingest:11800}
      SEED_INTERVAL_SECONDS: ${SEED_INTERVAL_SECONDS:-30}
      JVM_METRIC_INTERVAL_SECONDS: ${JVM_METRIC_INTERVAL_SECONDS:-60}
    volumes:
      - ./run/demo:/app
    depends_on:
      ai-apm-ingest:
        condition: service_healthy
    networks:
      apm:
        ipv4_address: 172.20.80.6

  ai-apm-demo-sw:
    image: ${LOCAL_JDK_IMAGE:-databuff-local/jdk-dev:17-jammy}
    container_name: ai-apm-demo-sw
    hostname: ai-apm-demo-sw
    restart: unless-stopped
    working_dir: /app
    entrypoint: ["/bin/sh", "/app/start.sh"]
    environment:
      TZ: Asia/Shanghai
      JAVA_TOOL_OPTIONS: -Xms64m -Xmx128m -Duser.timezone=Asia/Shanghai
      SEED_PROTOCOL: skywalking
      SKYWALKING_GRPC_TARGET: ai-apm-ingest:11800
      SEED_INTERVAL_SECONDS: 30
      JVM_METRIC_INTERVAL_SECONDS: 60
    volumes:
      - ./run/demo:/app
    depends_on:
      ai-apm-ingest:
        condition: service_healthy
    networks:
      apm:
        ipv4_address: 172.20.80.7

networks:
  apm:
    name: databuff-ai-apm-local
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.80.0/24
